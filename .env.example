# Copy to .env for local runs: `cp .env.example .env`, then set your key.
# The interview Codespace exports GOOGLE_API_KEY for you, so no .env is needed there.
export GOOGLE_API_KEY=
export ANTHROPIC_API_KEY=
